# 06 — Active TODO

## Priority queue
1. **Confirm retrieval baseline**
   - Run/extend deterministic routing tests against realistic sqlite fixtures.
2. **Seed real tag registry from actual usage**
   - Extract real tags from docs/fixtures/logs and prepare Arbiter review packet.
3. **Dry-run canonical chunk migration**
   - Execute DDL+transform dry run, capture provenance/plane violations, no destructive cutover.
4. **Narrow agent ownership boundaries**
   - Lock practical ownership scope for Jabari/Heru/Kimi/Arbiter in prompts/docs/manifests.
5. **Broader runtime wiring later**
   - Expand adapters and integration only after steps 1–4 stabilize.
6. **Consider Lobster / llm-task augmentation only after grounding is stable**
   - Treat as post-baseline enhancement, not current runtime truth.

## Explicitly deferred
- Treating vector search as authored truth.
- Declaring production retrieval complete without runtime-verified evidence.
- Silent schema or semantic replacement.
